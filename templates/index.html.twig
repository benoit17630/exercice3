{% extends "base.html.twig" %}
{% block titre %}Accueil {% endblock %}
{% block section %}
    <section class="w-100 flex bgindex">
        {% for article in last3article %}

                <div class="w-30 ">
                    <a class="none black" href="{{ path('article',{id:article.id}) }}"><h2>{{ article.title }}</h2></a>
                    <div><img src="{{ article.image }}" alt="" class="image"></div>
                </div>


        {% endfor %}

    </section>
    <div class=""><img src="{{ asset('assets/img/photo.jpg') }}" alt="" class="w"></div>
{% endblock %}