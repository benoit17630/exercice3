{% extends "base.html.twig" %}
{% block titre %}
{{ article.title }}
{% endblock %}
{% block section %}
    <div >
        <div class="text-center"><img src="{{ article.image }}" alt="{{ article.image }}" ></div>
        <h3 class="text-center">{{ article.title }}</h3>
        <p class="text-center">{{ article.content }}</p>

    </div>
{% endblock %}